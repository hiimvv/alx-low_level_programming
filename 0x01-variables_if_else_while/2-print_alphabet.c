#include <stdio.h>

/**
 * main - A program that prints the alphabetic letters
 * Return: Alaways 0
 */
int main(void)
{
	char a;
	for (a = 'a' ; a <= 'z' ; a++)
	putchar(a);
	putchar("\n");
	return (0);
}
