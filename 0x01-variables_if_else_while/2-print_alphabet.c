#include <stdio.h>

/**
 * main - A program that prints the alphabetic letters
 * Return: Alaways 0
 */
int main(void)

{
	char d;
	for (d = 'a' ; d <= 'z' ; d++)
	putchar(d);
	putchar('\n');
	return (0);
}
