#include <stdio.h>
/**
 * main - a program that prints a statement using printf functions
 * Return: 0 Success
 */
int main(void)
{
	prinf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
